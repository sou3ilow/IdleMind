# Line Guard Duel プログラム概要

`2b.html` は、Canvas だけで完結する 2 人対戦ミニゲームです。

## 全体構成
- HTML: 全画面 `canvas#game` を 1 つ配置。
- CSS: 画面全体を黒背景・スクロール無効・タッチ操作前提に設定。
- JavaScript(IIFE): ゲーム状態、物理、入力、描画、ループをすべて実装。

## 主要なゲームルール
1. 画面は上下 2 分割（上: red、下: blue）。
2. セットアップ中に各プレイヤーが 3 個ずつターゲットを配置。
3. ゲーム開始後はタップ 2 回で防御ラインを 1 本作成（最大 3 本）。
4. ボールがラインに当たると反射し、通常ラインは消える。
5. 相手のターゲット 3 個をすべて破壊すると勝利。
6. 中央マーカー通過で次のボールタイプへ変化（normal/speed/big/small/split）。

## 実装の要点
- 物理更新: `step(dt)` で移動、壁反射、中央判定、ライン衝突、ターゲット衝突を順に処理。
- 衝突: 線分と円の当たり判定（`segmentHitsCircle`）を使用。
- 特殊球:
  - `speed`: 速度倍率アップ
  - `big`: 大型化、1 回だけラインを破壊して通常球へ戻る
  - `small`: 小型・やや高速
  - `split`: 2 球化
- 演出: パーティクル、トレイル、中央マーカーの回転・ダッシュ表現。
- メインループ: `requestAnimationFrame` で `updateParticles` → `step` → `draw` を毎フレーム実行。

## 入力
- `pointerdown` のみ使用。
- 勝利表示中のタップで `reset()`。

